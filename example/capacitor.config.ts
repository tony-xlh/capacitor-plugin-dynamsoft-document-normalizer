import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dynamsoft.documentscanner',
  appName: 'Document Scanner',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
